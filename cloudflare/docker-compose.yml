services:
  cloudflare-ddns:
    image: timothyjmiller/cloudflare-ddns:latest
    container_name: cloudflare
    restart: unless-stopped
    env_file: .env
    deploy:
      resources:
        limits:
          memory: 64m